name: Build Firmware

on:
  push:
    branches: [master, main]
    paths:
      - 'SEEsDriver/**'
  pull_request:
    branches: [master, main]
    paths:
      - 'SEEsDriver/**'
  workflow_dispatch:  # Manual trigger

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Build firmware
        run: |
          cd SEEsDriver
          pio run

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: sees-firmware
          path: SEEsDriver/.pio/build/teensy41/firmware.hex
          retention-days: 90
